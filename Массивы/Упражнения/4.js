// #4 Соединение чисел

// Как с помощью метода join превратить массив [3, 2, 1] в строку "3 больше, чем 2 больше, чем 1"?

let arr = [3, 2, 1];

let result = arr.join(" больше, чем ");

console.log(result);